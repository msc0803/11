name: Build Windows EXE

on:
  push:
    branches: [ master, main ]
  workflow_dispatch:

jobs:
  build-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pyqt6 pandas openpyxl xlrd requests websockets pyinstaller numbers-parser pillow

      - name: Build EXE
        run: |
          python -m PyInstaller --noconfirm --onefile --windowed --name "视频素材批量查询工具" --icon "app_icon.ico" --add-data "api_core.py;." --add-data "app_icon.ico;." gui_app.py

      - name: Upload
        uses: actions/upload-artifact@v4
        with:
          name: 视频素材批量查询工具-Windows
          path: dist/视频素材批量查询工具.exe
